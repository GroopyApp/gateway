server:
  port: 8095
  address: localhost
spring:
  application.name: groopy-gateway
  mvc:
    dispatch-options-request: true

grpc:
  server:
    security:
      enabled: false
  client:
    userService:
                address: "static://68.183.125.135:8071"
                negotiationType: PLAINTEXT
    wallService:
                address: "static://68.183.125.135:8070"
                negotiationType: PLAINTEXT
    chatService:
                address: "static://68.183.125.135:8072"
                negotiationType: PLAINTEXT

groopy:
  app:
    apiKey:
      header: "x-api-key"
      value: ${GATEWAY_API_KEY}
